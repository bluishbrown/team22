import React, { Component } from 'react';
import data from "Tempsrc/MOCK_DATA.json";

const users = [data]


// https://www.daleseo.com/react-router-authentication/
// "예제용 인증 모듈" 부분부터 시작

export function signIn({ UserMail, UserPwd}) {
    const user = users.find(

    )
}